#include <stdio.h>
#include <stdint.h>

void return_array(const uint8_t *arrayIn, uint8_t *arrayOut, uint16_t size) {

}

int main(void) {
  uint8_t var;
  var = 0xFF;
  uint8_t *p;
  p = &var;

  printf("p = %p, &var = %p, var = %X\n", p, &var, var);
  return 0;
}
